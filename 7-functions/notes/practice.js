// write a function that takes in a string and removes any instances of an '!' or a space

function removeChar(str){
    newStr = ' ';
    for(let i = 0; i < str.length; i++){
        str[i] !== '!' || str[i] !== ' '
    }
    str[i] !== '!' || str[i] !== ' '
    return(newStr)
}
console.log(newStr)

// write a function that takes in an array of numbers and finds the average of them

const arrayOfNums = [12, 6, 87, 23, 9, 17]{
// new variable to put the average in
// add all the numbers and divide by the amount of numbers in the array 

function (findAverage){
    let average = // 
    let NewNum = []
    for(let i = 0; i < 0; i++)
    arrayOfNums[i] += arrayOfNums[i]/ 6

    // return(newNum)
}
}
// write a function that takes in a number as an input and removes all odd numbers from it

// set a large number with evens and odds in it
// create a function to loop through large number 
// create a new variable to 'keep' only even numbers in
// use a module to find remainers of 1 = your odd numbers 
// remove odd numbers
// return a value with only the even numbers 